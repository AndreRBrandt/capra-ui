/**
 * Capra UI - Todos os Componentes
 * ================================
 * Re-exporta todos os componentes do framework.
 *
 * @example
 * ```ts
 * // Import específico (tree-shakeable)
 * import { KpiCard, DataTable } from '@/components/analytics'
 * import { Modal, Popover } from '@/components/ui'
 *
 * // Import geral (conveniência)
 * import { KpiCard, Modal, AppShell } from '@/components'
 * ```
 */

// Analytics
export * from "./analytics";

// Charts
export * from "./charts";

// Containers
export * from "./containers";

// Layout
export * from "./layout";

// UI
export * from "./ui";

// Filters
export * from "./filters";
